import { beforeEach, afterEach } from 'vitest';

// Global test setup
beforeEach(() => {
  // Optional: Clear mocks or reset test environment
  // Example: jest.clearAllMocks()
});

afterEach(() => {
  // Optional: Cleanup after tests
  // Example: resetAllWireMocks()
});